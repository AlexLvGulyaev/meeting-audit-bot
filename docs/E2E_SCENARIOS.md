# E2E-сценарии — Meeting Audit Bot

Документ описывает ручные E2E-проверки, которые проходились перед публикацией кейса. Все скриншоты лежат в `docs/screenshots/`, индекс — в `docs/MEDIA_INDEX.md`.

## Предварительные условия

- Проект развёрнут по `DEPLOYMENT_GUIDE.md`.
- Telegram-бот доступен и отвечает на `/start`.
- Админка доступна по `/admin/login`.
- Активный промпт по умолчанию — `onboarding`.
- E2E выполнялись в Chrome / Firefox на десктопе и в мобильном Telegram.

---

## 1. Telegram `/start` — onboarding и список сценариев

**Цель:** пользователь видит доступные сценарии аудита и активный промпт.

**Шаги:**
1. Открыть Telegram, найти бота `meeting-audit-bot`.
2. Отправить `/start`.
3. Убедиться, что бот прислал приветствие и список сценариев.

**Ожидаемый результат:**
- В списке 4 сценария с русскими названиями.
- Активный сценарий помечен ✅.
- Указано, кто может сменить активный промпт.

**Скриншот:** `docs/screenshots/tg-start-scenarios.png`

---

## 2. Telegram — отправка аудио и получение аудита sales-call

**Цель:** боевой проход аудита реалистичного B2B-звонка.

**Шаги:**
1. В админке `/admin` сменить активный промпт на `sales-call`.
2. В Telegram отправить тестовый аудиофайл (`examples/sample-b2b-sales-call-retail.ogg` или аналог).
3. Дождаться сообщения с аудитом.

**Ожидаемый результат:**
- Markdown-форматирование рендерится корректно (заголовки, жирный текст, списки).
- Нет обёртки ```Markdown ... ```.
- Итоговая оценка 87,5% (7 ✅, 1 ⚠️ по критерию «Следующий шаг»).
- Последняя строка — вердикт «Рекомендую доработать.».

**Скриншот:** `docs/screenshots/tg-audio-sales-call-audit.png`

---

## 3. Telegram `/help` — справка

**Цель:** справка содержит инструкцию и список сценариев.

**Шаги:**
1. Отправить `/help`.

**Ожидаемый результат:**
- Пошаговая инструкция.
- Список доступных сценариев.
- Указан активный промпт.

**Скриншот:** `docs/screenshots/tg-help.png`

---

## 4. Админка — авторизация

**Цель:** вход по токену работает.

**Шаги:**
1. Открыть `/admin/login`.
2. Ввести `ADMIN_TOKEN`.
3. Нажать «Войти».

**Ожидаемый результат:**
- Редирект на `/admin`.
- В сайдбаре видно имя/роль пользователя.

**Скриншот:** `docs/screenshots/admin-login.png`

---

## 5. Админка — dashboard

**Цель:** dashboard показывает состояние системы.

**Шаги:**
1. Открыть `/admin` после авторизации.

**Ожидаемый результат:**
- Видны статусы OpenAI и GigaChat (ключ задан / не задан).
- Активный промпт отображается с human-readable названием.
- Метрики успешных/неуспешных обработок.

**Скриншот:** `docs/screenshots/admin-dashboard.png`

---

## 6. Админка — смена активного промпта

**Цель:** администратор может переключить сценарий аудита.

**Шаги:**
1. На `/admin` кликнуть селектор «Активный сценарий аудита».
2. Выбрать другой промпт, например `sales-call`.
3. Убедиться, что название отображается человекочитаемо.

**Ожидаемый результат:**
- Список раскрыт.
- Новый промпт выделен/подсвечен.
- После сохранения появляется подтверждение.

**Скриншот:** `docs/screenshots/admin-change-prompt.png`

---

## 7. Админка — список сессий

**Цель:** оператор видит все обработки в одном списке.

**Шаги:**
1. Открыть `/admin/executions`.

**Ожидаемый результат:**
- Список сессий с глобальной нумерацией (#1 — самая старая, #N — самая новая).
- Видны статус, дата, пользователь.

**Скриншот:** `docs/screenshots/admin-executions-list.png`

---

## 8. Админка — детализация сессии

**Цель:** можно посмотреть одну сессию со всеми артефактами.

**Шаги:**
1. На `/admin/executions` выбрать последнюю сессию.

**Ожидаемый результат:**
- Аудио-плеер с загруженным файлом.
- Панель «Запрос пользователя» с началом транскрипта.
- Панель «Ответ системы» с началом аудита.
- Кнопки «Развернуть» в строке с заголовками.

**Скриншот:** `docs/screenshots/admin-execution-detail.png`

---

## 9. Админка — полный транскрипт

**Цель:** транскрипт доступен для просмотра целиком.

**Шаги:**
1. В детализации сессии нажать «Развернуть» у панели «Запрос пользователя».

**Ожидаемый результат:**
- Открывается модальное окно или развёрнутая панель с полным текстом.
- Видны метки спикеров (Speaker A / Speaker B).

**Скриншот:** `docs/screenshots/admin-execution-transcript-full.png`

---

## 10. Админка — полный аудит

**Цель:** аудит доступен для просмотра целиком.

**Шаги:**
1. В детализации сессии нажать «Развернуть» у панели «Ответ системы».

**Ожидаемый результат:**
- Полный Markdown-аудит без обёртки ```Markdown ... ```.
- Видны заголовки, списки, статусы.

**Скриншот:** `docs/screenshots/admin-execution-audit-full.png`

---

## 11. Админка — security audit log

**Цель:** фиксируются действия администраторов.

**Шаги:**
1. Выполнить login, config_update, provider_test.
2. Открыть `/admin/audit`.

**Ожидаемый результат:**
- Таблица с событиями: login, config_update, provider_test.
- Колонки: пользователь, роль, IP, дата/время, действие.

**Скриншот:** `docs/screenshots/admin-audit-log.png`

---

## 12. Админка — детализация аудит-события

**Цель:** можно раскрыть подробности одного события.

**Шаги:**
1. На `/admin/audit` кликнуть по строке события.

**Ожидаемый результат:**
- Правая панель показывает: ID, имя, роль, IP, дату, детали.

**Скриншот:** `docs/screenshots/admin-audit-detail.png`

---

## 13. Админка — demo read-only

**Цель:** demo-вход не позволяет менять настройки.

**Шаги:**
1. Выйти из admin.
2. Войти по `ADMIN_DEMO_TOKEN`.
3. Открыть `/admin`.

**Ожидаемый результат:**
- Поля формы disabled.
- Кнопки «Сохранить» / «Проверить» недоступны.

**Скриншот:** `docs/screenshots/admin-demo-readonly.png`

---

## 14. Админка — тест провайдера

**Цель:** админ может проверить работоспособность LLM-провайдера.

**Шаги:**
1. На `/admin` нажать «Проверить» рядом с OpenAI или GigaChat.

**Ожидаемый результат:**
- Появляется toast-уведомление внизу консоли с результатом.
- При успехе — `test=ok`, при ошибке — `test=err` с описанием.

**Скриншот:** `docs/screenshots/admin-provider-test.png`

---

## Опционально: дневной лимит обработок в Telegram

**Цель:** не-админ не может превысить квоту.

**Шаги:**
1. С неадминского Telegram-аккаунта отправить 6 аудио/видео за день.

**Ожидаемый результат:**
- На 6-й отправке бот отвечает: «Лимит обработок на сегодня исчерпан (5). Попробуйте завтра.»

**Скриншот:** `docs/screenshots/tg-daily-limit.png` (не создан, опционально)
